import 'package:flutter/material.dart';

class autoZoom extends StatelessWidget {
  const autoZoom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Descripción del Automovil'),
      ),
    );
  }
}